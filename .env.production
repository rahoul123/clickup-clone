# Vite picks this up automatically during `vite build` (mode=production).
# A relative base means the packaged desktop app works no matter which port
# the bundled backend ends up listening on — fetch('/api/...') just hits
# the same origin that served the HTML.
VITE_API_BASE_URL="/api"
